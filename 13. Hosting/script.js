
console.log(username)

var username = 'debu';

// In var case, variable we can accessed everything. first we log username and then access the variables.

// in const or let case , we can't access value everything. first we can create a variable and then print this.

// This is called Temporal Dead Zone(TDZ). :- A temporal dead zone is the area of a block where a variable is inaccissible untill the moment the computer completly initializes it.